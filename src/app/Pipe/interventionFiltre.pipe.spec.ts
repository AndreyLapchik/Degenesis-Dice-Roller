/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { InterventionFiltrePipe } from './interventionFiltre.pipe';

describe('Pipe: InterventionFiltree', () => {
  it('create an instance', () => {
    let pipe = new InterventionFiltrePipe();
    expect(pipe).toBeTruthy();
  });
});
