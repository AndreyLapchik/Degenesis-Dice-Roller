/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { InterventionSearchPipe } from './interventionSearch.pipe';

describe('Pipe: InterventionSearche', () => {
  it('create an instance', () => {
    let pipe = new InterventionSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
